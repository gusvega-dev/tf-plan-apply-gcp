{
  "name": "tf-plan-apply-gcp",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test test/*.test.mjs", "test:integration": "node --test test/*.integration.mjs" },
  "engines": { "node": ">=22" }
}
